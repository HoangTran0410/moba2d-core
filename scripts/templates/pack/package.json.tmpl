{
  "name": "@moba2d/content-__PACK_ID__",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "check-seams": "moba2d-check-seams ./spells",
    "build": "vite build && node scripts/write-manifest.mjs",
    "manifest:write": "node scripts/write-manifest.mjs",
    "verify": "npm run typecheck && npm run check-seams && npm test && npm run build"
  },
  "devDependencies": {
    "@moba2d/core": "__CORE_SPEC__",
    "@types/node": "^22.20.1",
    "@types/p5": "^1.7.6",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.6.1"
  }
}
